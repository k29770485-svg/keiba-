# {スキル名} 実演レポート

**実施日**: {日付} / **作成**: Manus AI
**対象スキル**: `{skill-name}`（{一行説明}）

---

## 概要

{何を検証したかを2〜3段落で。以下を必ず含める}
- モックではなく実データ・実環境を使ったこと（規模を数値で: 対象N件、環境の具体名）
- 対象スキルの中心的な主張は何か（1〜2点に絞る）
- その主張が実測で成立したか

![{効果とコストのグラフ}]({chart.png})

---

## デモ1: {前提条件の検証}

{契約テスト・環境確認など、実装前に確かめるべきことがスキルに定義されている場合は最初に置く}

| 検証項目 | 結果 |
|---|---|
| {項目} | OK / NG |

{単に「全部OKだった」で終わらせず、設計として優れている点を1つ指摘する。
たとえば「存在しないこと」を積極的に検証している、など}

---

## デモ2〜N: {各段階の実行}

{段階ごとに1セクション。各セクションに必ず以下を入れる}

**投入前後の数値**

| 項目 | 実行前 | 実行後 | 情報源・手段 |
|---|---|---|---|
| {項目} | 0.0% | **100.0%** | {どうやって埋めたか} |

**コスト**: 外部リクエスト{N}件 / 所要{N}ms

**取得された実データの例**（3〜5件を表で示す。抽象論ではなく実物を見せる）

| {列} | {列} | {列} |
|---|---|---|

{パーサやロジックの細かな配慮が実際に効いた箇所を具体的に指摘する。
例:「イレジンの性別が正しく『セ』と判定されているのは全角スペースでトークン分割しているため」}

---

## デモN: {安全機構の対照実験} — 最も価値のある検証

{スキルが「これが無いと壊れる」と主張する機構について、破壊条件を仕込んで両方を実行}

### 機構あり

```
{実際の出力ログを貼る}
```

{状態が変化しなかったことを示す。副次的な利点（コスト削減など）があれば指摘}

### 機構なし

| {識別子} | {書き込まれた値} | {書き込まれた値} |
|---|---|---|
| {実際は別物のデータが入った例} | **{誤った値}** | **{誤った値}** |

{ここが最重要。「例外もログも出ない」「気付けない」という障害の性質を明示する}

---

## 総括

### 実測されたコスト効率

| 段階 | 手法 | コスト | 到達状態 |
|---|---|---|---|
| {段階} | {手法} | **{N}件** | {結果} |
| — | 参考: {非推奨手法} | {N}件 | — |

{合計値と、推奨順序を守らなかった場合の比較を1段落で}

### スキルとして優れている点

{単なる手順書との違いを述べる。「失敗の仕方を知っている」タイプの知見が
含まれているか、それが実行可能なスクリプト/テストとして提供されているか}

### 運用上の補足

{スケールさせた時の注意点、今回のデモでは再現しなかった条件（レート制限など）、
規模を絞ったことによる限界を正直に書く}

{データ出典があれば明記する}

---

## 成果物

| ファイル | 内容 |
|---|---|
| {file} | {内容} |

## 参考

[1] [{出典名}]({URL}) — {何に使ったか}
